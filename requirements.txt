# Dependencias del proyecto, con version exacta.
#
# Se fija la version a proposito: "pytest" a secas instala la ultima que haya
# ese dia, y dos maquinas acabarian con versiones distintas. Un fallo que solo
# aparece en una de las dos es de los que mas cuesta encontrar.
#
# Se anotan solo las que se piden directamente. Lo que ellas arrastren viene
# solo; no hace falta escribirlo aqui.

pytest==9.1.1

# El servidor (paso 2). `fastapi` recibe las peticiones y `uvicorn` es quien de
# verdad escucha en el puerto: FastAPI describe QUE contestar, uvicorn se ocupa
# de estar encendido. `httpx2` lo pide el TestClient para probar las rutas sin
# levantar un servidor de verdad — con el `httpx` viejo funciona igual, pero
# avisa de que esta descontinuado.
fastapi==0.141.1
uvicorn==0.52.1
httpx2==2.9.1
