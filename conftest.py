# Este archivo está vacío a propósito, pero hace falta.
#
# Al encontrarlo, pytest añade esta carpeta —la raíz del proyecto— a la lista
# de sitios donde Python busca módulos. Sin él, los tests no encontrarían `app`
# y fallarían con "ModuleNotFoundError" antes de probar nada.
